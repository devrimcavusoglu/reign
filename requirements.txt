# Runtime dependencies for the `reign` package and the evaluation scripts.
# Mirrors [project.dependencies] in pyproject.toml. For a full install use
# `pip install -e .` (add `[eval]` for the MTEB harness, `[dev]` for tooling).
torch>=2.1
transformers>=4.51
lightning>=2.5
datasets>=2.14
huggingface-hub>=0.23
h5py>=3.8
numpy>=1.24
tqdm>=4.65

# Sparse retrieval baselines (scripts/evaluate_sparse_baselines.py)
scikit-learn>=1.3
bm25s>=0.2

# Optional: MTEB evaluation harness (`pip install -e ".[eval]"`)
# mteb>=1.38.9

# Optional: TensorBoard metric logging (`--logger tensorboard`)
# tensorboard>=2.15
